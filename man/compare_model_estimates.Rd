% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_model_estimates.R
\name{compare_model_estimates}
\alias{compare_model_estimates}
\title{Compare Parameter Estimates Across Multiple Models}
\usage{
compare_model_estimates(
  ...,
  standardized = FALSE,
  ci = 0.95,
  component = "all",
  verbose = TRUE
)
}
\arguments{
\item{...}{Two or more fitted model objects to compare.}

\item{standardized}{Controls standardized extraction. Accepted values are
\code{FALSE}, \code{TRUE}, \code{"std.all"}, \code{"std.lv"}, \code{"std.nox"}, plus aliases:
\code{"all"}, \code{"latent"}, \code{"lv"}, and \code{"no_exogenous"}.}

\item{ci}{Confidence level for interval columns. Must be a numeric value in
\verb{(0, 1)}.}

\item{component}{Character vector indicating which parameter components to
keep. Use \code{"all"} (default) to keep all available components.}

\item{verbose}{Logical; if \code{TRUE}, prints informative messages emitted by
the underlying extraction methods.}
}
\value{
A data frame with class \code{compare_model_estimates}. The result
contains parameter identity columns (\code{Group}, \code{Level}, \code{Component}, \code{To},
\code{Operator}, \code{From}) plus model-specific estimate columns such as
\verb{Coefficient.<model>}, \verb{SE.<model>}, \verb{CI_low.<model>}, \verb{CI_high.<model>},
\verb{z.<model>}, \verb{p.<model>}, and \verb{converged.<model>}.
}
\description{
\code{compare_model_estimates()} compares parameter estimates from two or more
fitted models. It extracts estimates via \code{\link[=model_estimates]{model_estimates()}} and aligns
parameters by their structural identity so that shared and model-specific
parameters can be displayed side by side.

The current implementation is designed for fitted \code{lavaan} objects and
integrates with the package formatting and export workflow through
\code{prepare_table()}, \code{\link[=format_results]{format_results()}}, and \code{\link[=save_table]{save_table()}}.
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  library(lavaan)
  library(psymetrics)

  hs_cfa_model <- '
    visual =~ x1 + x2 + x3
    textual =~ x4 + x5 + x6
    speed =~ x7 + x8 + x9
  '

  hs_sem_model <- '
    visual =~ x1 + x2 + x3
    textual =~ x4 + x5 + x6
    speed =~ x7 + x8 + x9
    textual ~ visual
    speed ~ visual + textual
  '

  fit1 <- cfa(hs_cfa_model, data = HolzingerSwineford1939)
  fit2 <- sem(hs_sem_model, data = HolzingerSwineford1939)

  compared <- compare_model_estimates(CFA = fit1, SEM = fit2)
  compared
  format_results(compared, table_args = list(select = "ci_p2"))
} else {
  message("Please install 'lavaan' to run this example.")
}
}
\seealso{
\code{\link[=model_estimates]{model_estimates()}} for single-model extraction and
\code{\link[=format_results]{format_results()}} for output customization.
}
