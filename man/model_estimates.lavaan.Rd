% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_estimates-lavaan.R
\name{model_estimates.lavaan}
\alias{model_estimates.lavaan}
\title{Extract Parameter Estimates from a lavaan Model}
\usage{
\method{model_estimates}{lavaan}(
  fit,
  standardized = FALSE,
  ci = 0.95,
  component = "all",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{fit}{A fitted \code{lavaan} object (for example from
\code{lavaan::cfa()}, \code{lavaan::sem()}, or \code{lavaan::growth()}).}

\item{standardized}{Controls standardized extraction.
Accepted values are \code{FALSE}, \code{TRUE}, \code{"std.all"},
\code{"std.lv"}, \code{"std.nox"}, plus aliases:
\code{"all"}, \code{"latent"}, \code{"lv"}, and \code{"no_exogenous"}.}

\item{ci}{Confidence level for interval columns. Must be a
numeric value in \verb{(0, 1)}.}

\item{component}{Character vector indicating which parameter
components to keep. Use \code{"all"} (default) to keep all
available components.}

\item{verbose}{Logical; if \code{TRUE}, prints informative messages
about non-convergence and partially unavailable inferential
statistics.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A data frame with class \code{model_estimates}.
Core columns are:
\code{To}, \code{Operator}, \code{From}, \code{Coefficient}, \code{SE}, \code{CI_low},
\code{CI_high}, \code{z}, \code{p}, \code{Component}, and \code{converged}.
}
\description{
\code{model_estimates.lavaan()} extracts CFA/SEM parameter
estimates from a fitted \code{lavaan} object and organizes
them into components (for example, loadings, regressions,
variances, and thresholds). The output is designed to
integrate with \code{psymetrics} formatting and export methods.
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  library(lavaan)
  library(psymetrics)

  sem_model <- '
    ind60 =~ x1 + x2 + x3
    dem60 =~ y1 + y2 + y3 + y4
    dem65 =~ y5 + y6 + y7 + y8
    dem60 ~ ind60
    dem65 ~ ind60 + dem60
  '

  fit <- sem(sem_model, data = PoliticalDemocracy)
  model_estimates(fit)
  model_estimates(fit, component = c("loading", "regression"))
  model_estimates(fit, standardized = "std.lv")
} else {
  message("Please install 'lavaan' to run this example.")
}
}
\seealso{
\code{\link[=model_estimates]{model_estimates()}} for the generic.
}
