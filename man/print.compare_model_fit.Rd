% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_model_fit-methods.R
\name{print.compare_model_fit}
\alias{print.compare_model_fit}
\title{Print Method for compare_model_fit Objects}
\usage{
\method{print}{compare_model_fit}(x, digits = 3, ci_digits = digits, p_digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{compare_model_fit}, typically
created by the \code{\link{compare_model_fit}} function.}

\item{digits}{Number of digits for rounding numeric values.
Default is 3.}

\item{ci_digits}{Number of digits for rounding confidence
intervals. Default is \code{digits}.}

\item{p_digits}{Number of digits for rounding p-values.
Default is 3.}

\item{...}{Other arguments are ignored.}
}
\value{
Returns \code{x} invisibly.
}
\description{
\code{print.compare_model_fit()} prints a text summary of
compare-model-fit tables to the console. By default, \code{Chi2}
is shown with 2 decimals while other indices use \code{digits},
and fractional chi-square df values are rounded to 2 decimals
in the \code{Chi2(df)} header. For markdown or HTML output (or to
override column-specific digits), use \code{\link[=format_results]{format_results()}}.
}
\examples{
if (requireNamespace("lavaan", quietly = TRUE)) {
  library(psymetrics)
  library(lavaan)

  hs_model <- 'visual  =~ x1 + x2 + x3
               textual =~ x4 + x5 + x6
               speed   =~ x7 + x8 + x9'
  fit1 <- cfa(hs_model, data = HolzingerSwineford1939,
              estimator = "ML")
  fit2 <- cfa(hs_model, data = HolzingerSwineford1939,
              estimator = "MLR")
  comparison <- compare_model_fit(fit1, fit2)
  comparison
  print(comparison, digits = 4)
  html_table <- format_results(comparison, output = "html")
  if (interactive()) {
    html_table
  }
} else {
  message("Please install 'lavaan' to run this example.")
}
}
\seealso{
\code{\link[=format_results]{format_results()}}
}
